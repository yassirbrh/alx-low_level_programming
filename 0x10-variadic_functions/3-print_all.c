#include <stdio.h>
#include <stdarg.h>
#include "variadic_functions.h"
/**
 * print_all - Function
 *
 * Description: Prints anything.
 *
 * @format: The data type of arguments.
 *
 * Return: No return because it's a void function.
 */
void print_all(const char * const format, ...)
{
	int i;

	i = 0;
	while (*(format + i) != '\0')
	{
		if ()
		i++;
	}
}
